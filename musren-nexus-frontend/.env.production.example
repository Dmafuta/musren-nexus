# Copy to .env.production and fill in values before running: npm run build
# VITE_API_URL is NOT needed — the Cloudflare Worker passes /api/* to the VPS origin automatically.

VITE_SUPABASE_URL=https://hojqiymudpnjyyqeqvrl.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
